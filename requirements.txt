# Agente que dirige o browser (CDP) + LLMs, incluindo AWS Bedrock
browser-use[aws]==0.13.7
uv==0.11.23

# Requerido pelo provider de credenciais "bearer token" do botocore quando
# autenticando no Bedrock via Bedrock API key (AWS_BEARER_TOKEN_BEDROCK).
# Sem isso: MissingDependencyException ao chamar bedrock-runtime.
awscrt==0.31.2

# Orquestração (state machine, loop por paciente)
langgraph==1.2.4

# Conector Gemini usado pelo browser-use quando LLM_PROVIDER=gemini
langchain-google-genai==4.2.4

# Integrações
boto3==1.42.37
gspread==6.2.1
python-dotenv==1.2.2
pydantic==2.12.5
pypdf==6.10.2

# Dev/testes
pytest==9.0.1
